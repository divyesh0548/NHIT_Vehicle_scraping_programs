I want you to develop a python program which will merge two excel files (which will have same column names) and upload it to S3 cloud. Take id as input for the table nhit_vehicles. For that id, remove the file stored in "output_file_s3_url" from table as well as S3 cloud. And upload merged output and save it's URL in that column. For S3 key take the same file path as saved in that ouput column and upload the file.